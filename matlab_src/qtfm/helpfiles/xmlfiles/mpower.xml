<?xml version="1.0" encoding="UTF-8" standalone="no" ?>
<!DOCTYPE function SYSTEM "qtfmfunction.dtd">
<function name = "mpower" overload = "both">
<short>^ Matrix power</short>
<syntax>Z = X ^ Y</syntax>
<long>
<para>
<code>X ^ Y</code> raises the quaternion matrix <code>X</code> to the
power <code>Y</code>.
</para>
    <para>
        If both parameters are scalar, the <code>.^</code>
    function is called. Otherwise, for real integer <code>Y</code> (including
    negative integers), quaternion matrices are raised to the given power.
    This is done by repeated squaring. For non-integer <code>Y</code>, a
    matrix logarithm/exponential formula is used. CAUTION, this may not
    give correct results in all cases.
</para>
<para>
    In the octonion case, the power of a matrix is ambiguous for <code>Y > 2</code>
    (octonion matrix multiplication is not power associative, unlike octonion
    multiplication). Hence only integer powers of 0, 1 and 2 are implemented.
</para>
</long>
<seealso>
<matlabfun name = "mpower"/>
<matlabfun name = "power"/>
<qtfmfun name = "power"/>
</seealso>
</function>

<!--$Id: mpower.xml 1173 2022-12-28 18:46:45Z sangwine $-->
